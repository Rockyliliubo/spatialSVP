% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_svp_data.R
\name{simulate_svp_data}
\alias{simulate_svp_data}
\title{Simulate spatial transcriptomics data with spatially variable gene sets}
\usage{
simulate_svp_data(
  n_spots = 900L,
  n_genes = 120L,
  n_sets = 12L,
  n_spatial_sets = 4L,
  set_size = 10L,
  n_blocks = 1L,
  pattern = c("bump", "gradient", "mix"),
  effect = 1.5,
  noise = 0.6,
  coord_scale = 1,
  seed = NULL
)
}
\arguments{
\item{n_spots}{Number of spots (placed on the smallest square grid that
fits, then jittered).}

\item{n_genes}{Total number of genes; must be at least
\code{n_sets * set_size}.}

\item{n_sets}{Total number of gene sets.}

\item{n_spatial_sets}{Number of sets carrying a spatial signal.}

\item{set_size}{Number of genes per set.}

\item{n_blocks}{Number of coordinate blocks (vertical strips); spots are
shuffled/analyzed within blocks when the returned \code{block} is passed
on. 1 = single section.}

\item{pattern}{Signal shape: \code{"bump"}, \code{"gradient"}, or
\code{"mix"} (half bump, half gradient across the spatial sets).}

\item{effect}{Signal amplitude multiplier (log space).}

\item{noise}{Gene-level log-space noise sd.}

\item{coord_scale}{Multiplier applied to the grid coordinates (default 1,
i.e. unit spacing). Use e.g. \code{276} to mimic Visium pixel
coordinates; signal shapes and block assignments are defined on the
unit grid and scale along with it, so results are directly comparable
across coordinate magnitudes.}

\item{seed}{Optional random seed.}
}
\value{
A list with \code{counts} (genes x spots matrix),
  \code{coords} (data.frame), \code{gene_sets} (tidy data.frame:
  source/target/mor), \code{block} (factor or NULL), and \code{truth}
  (data.frame: set, spatial, pattern, center_x, center_y).
}
\description{
Generates a toy spatial transcriptomics experiment on a jittered grid:
a fraction of gene sets carries a shared spatial signal (Gaussian bump or
linear gradient), the rest are spatially null. Used throughout the package
tests and vignette for power / type-I-error calibration.
}
\examples{
sim <- simulate_svp_data(n_spots = 200, n_genes = 80, n_sets = 6,
                         n_spatial_sets = 2, set_size = 8, seed = 1)
table(sim$truth$spatial)

}
